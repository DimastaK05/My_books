<div class="row">
  <div class="col-md-8">
    <div class="card mt-4">
      <div class="card-header d-flex justify-content-between align-items-center">
        <h4 class="mb-0"><i class="bi bi-tags"></i> Мои жанры</h4>
        <%= link_to new_genre_path, class: "btn btn-primary btn-sm" do %>
          <i class="bi bi-plus"></i> Добавить жанр
        <% end %>
      </div>
      <div class="card-body">
        <% if @genres.any? %>
          <div class="row">
            <% @genres.each do |genre| %>
              <div class="col-md-4 mb-3">
                <div class="card">
                  <div class="card-body">
                    <h5 class="card-title"><%= genre.name %></h5>
                    <p class="card-text">
                      <small class="text-muted">
                        Книг: <%= genre.books.count %>
                      </small>
                    </p>

                    <% if genre.books.any? %>
                      <div class="alert alert-warning py-1 mb-2">
                        <small>
                          <i class="bi bi-exclamation-triangle"></i>
                          Используется в <%= genre.books.count %> книг(ах)
                        </small>
                      </div>
                      <button class="btn btn-sm btn-outline-danger w-100" disabled>
                        <i class="bi bi-trash"></i> Нельзя удалить
                      </button>
                    <% else %>
                      <%= button_to genre_path(genre), method: :delete,
                                    class: "btn btn-sm btn-outline-danger w-100",
                                    data: { confirm: "Удалить жанр '#{genre.name}'?" } do %>
                        <i class="bi bi-trash"></i> Удалить
                      <% end %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-4">
            <i class="bi bi-tags display-1 text-muted"></i>
            <h5 class="mt-3">Жанры не добавлены</h5>
            <p class="text-muted">Добавьте жанры для сортировки книг</p>
            <%= link_to "Добавить первый жанр", new_genre_path, class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-md-4">
    <div class="card mt-4">
      <div class="card-header">
        <h5 class="mb-0"><i class="bi bi-info-circle"></i> О жанрах</h5>
      </div>
      <div class="card-body">
        <p>Жанры помогают организовать вашу библиотеку.</p>
        <ul>
          <li>Создавайте свои жанры</li>
          <li>Присваивайте жанры книгам</li>
          <li>Фильтруйте книги по жанрам</li>
        </ul>

        <div class="alert alert-info">
          <h6><i class="bi bi-lightbulb"></i> Важно:</h6>
          <p class="mb-0 small">
            Жанр нельзя удалить, если он используется в книгах.
            Сначала измените или удалите книги этого жанра.
          </p>
        </div>
      </div>
    </div>
  </div>
</div>