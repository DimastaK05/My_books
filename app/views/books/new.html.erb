<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card mt-4">
      <div class="card-header">
        <h4 class="mb-0"><i class="bi bi-plus-circle"></i> Добавить новую книгу</h4>
      </div>
      <div class="card-body">
        <%= form_with model: @book, local: true do |f| %>
          <div class="row">
            <div class="col-md-6">
              <div class="mb-3">
                <%= f.label :title, "Название книги *", class: "form-label" %>
                <%= f.text_field :title, class: "form-control", placeholder: "Введите название", required: true %>
              </div>
            </div>

            <div class="col-md-6">
              <div class="mb-3">
                <%= f.label :author, "Автор *", class: "form-label" %>
                <%= f.text_field :author, class: "form-control", placeholder: "ФИО автора", required: true %>
              </div>
            </div>
          </div>

          <div class="mb-3">
            <%= f.label :genre_id, "Жанр", class: "form-label" %>
            <%= f.select :genre_id,
                         options_from_collection_for_select(@genres, :id, :name, @book.genre_id),
                         { include_blank: "Выберите жанр..." },
                         class: "form-select" %>
            <div class="form-text">
              Нет нужного жанра? <%= link_to "Создать новый жанр", new_genre_path %>
            </div>
          </div>

          <div class="d-flex justify-content-between">
            <%= link_to "Отмена", books_path, class: "btn btn-secondary" %>
            <%= f.submit "Добавить книгу", class: "btn btn-success" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>